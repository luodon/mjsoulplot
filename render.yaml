services:
  - type: web
    name: mjsoul-pt-plot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
    repo: https://github.com/luodon/mjsoulplot
    branch: main
